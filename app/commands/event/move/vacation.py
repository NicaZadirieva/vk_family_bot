from app.commands.base import ICommand


class MoveVacationEventCmd(ICommand):
    """
    Команда родителя: “/move_vacation деревня на чт 17:00”.
    Если событие регулярное, переносится конкретный экземпляр на указанную дату без изменения шаблона.
    Для изменения всего шаблона используется уточняющий запрос бота.
    Перенос реализуется через запись в EventException,
    которая для конкретной даты задаёт новое время начала и окончания
    """
