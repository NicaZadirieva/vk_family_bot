from app.commands.base import ICommand


class ChooseChildCmd(ICommand):
    """Переключение между профилями детей"""

    """Родитель может выбрать активного ребёнка для последующих операций 
    	(назначение заданий, начисление баллов).
		Команда: кнопка “Выбрать ребёнка” → список детей; выбор запоминается до смены.
		Текущий активный ребёнок отображается в тексте меню или статусе.
		Активный ребёнок хранится в Redis (TTL 1 час)
	"""
